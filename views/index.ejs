<% if (!user) { %>
  <div class="hero-unit">
    <h1>GSSC Online Training Platform</h1>
    <p></p>
    <p class="text-center">
      <a class="btn btn-primary btn-large" href="/login"><%= __('Login')%></a>
      <a class="btn btn-large" href="/reg"><%= __('Register')%></a>
    </p>
  </div>
<% } else { %>
  <%- include educationhome %> 
<% } %>
